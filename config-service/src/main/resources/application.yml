server:
  port: 8888

spring:
  application:
    name: config_server
  cloud:
    config:
      server:
        bootstrap: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka